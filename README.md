# Power BI DAX Functions & Data Model Overview

This repository contains examples of essential DAX functions used in Power BI along with the data model structure reference.  
The content is divided into **general DAX functions**, **time-intelligence DAX functions**, and **the schema (table model)**.

---

## 📌 DAX1 — All Common DAX Functions

This section covers frequently used DAX functions such as:
- `SUM()`, `AVERAGE()`, `COUNTROWS()`
- `CALCULATE()`, `FILTER()`, `ALL()`
- `DIVIDE()`, `DISTINCTCOUNT()`
- `SUMX()`, `AVERAGEX()` and more...

> **Reference screenshot:**  
![DAX1](./DAX1.png)

---

## ⏳ DAX2 — Time Intelligence Functions (MTD, QTD, YTD, WTD Only)

This section focuses **only on the 4 major time-intelligence functions**:

| Function | Meaning | Example Usage |
|----------|---------|----------------|
| `MTD()`  | Month to Date | Calculate monthly cumulative totals |
| `QTD()`  | Quarter to Date | Evaluate values from start of quarter |
| `YTD()`  | Year to Date | Cumulative totals from start of year |
| `WTD()`  | Week to Date | Weekly accumulated values |

> **Reference screenshot:**  
![DAX2](./DAX2.png)

---

## 📊 Table Model — Schema Reference

This section displays the **relationship diagram / star schema** used in the model.

> **Reference screenshot:**  
![TABLEMODEL](./TABLEMODEL.png)

---

## 🧾 Author

**UTKARSH KOHLI**

